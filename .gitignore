# Rust / Soroban
target/
**/*.rs.bk

# Node
node_modules/
dist/
*.local

# Host tooling
.vercel/

# Secrets — Stellar identities live in ~/.config/stellar, never in the repo
.env
.env.*
!.env.example
.stellar/

# OS / editors
.DS_Store
.idea/
*.swp

# Internal-only copy of the design report (full text, incl. redacted sections)
docs/*.internal.md
